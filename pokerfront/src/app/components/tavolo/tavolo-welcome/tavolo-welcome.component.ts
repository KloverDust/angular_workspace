import { Component } from '@angular/core';

@Component({
  selector: 'app-tavolo-welcome',
  standalone: true,
  template: '',  // Empty template since the parent component already shows what we need
  styles: ''
})
export class TavoloWelcomeComponent {} 